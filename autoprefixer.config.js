const TARGET_BROWSERS = [
  'last 2 versions',
  'ie >= 11',
  'ios >= 10.3.3',
  'android >= 5.0.1'
]

const AUTOPREFIXER_SETTINGS = {
  browsers: TARGET_BROWSERS,
  grid: true
}

module.exports = AUTOPREFIXER_SETTINGS;
